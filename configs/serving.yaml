model_config: configs/model.yaml

faiss:
  index_path: ${FAISS_INDEX_PATH}
  embedding_dim: 512
  top_k: 5

api:
  host: ${API_HOST}
  port: ${API_PORT}

medasr:
  enabled: true
  model_id: Cnam-LTL/whisper-medical-fr  # placeholder — swap with actual MedASR
