services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    image: ai-medical-dept-backend:latest
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - ../.env
    restart: unless-stopped
